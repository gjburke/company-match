export default function Understanding() {
    return (
        <div>
            I have a pretty solid grasp on the React Basics, up to around hooks.

            But in general, I haven't worked with too many advanced systems or large React apps.

            I heard they get pretty complicated when you have a huge site with a lot of functionality,
            but that's why we're keeping it simple here.
        </div>
    );
}