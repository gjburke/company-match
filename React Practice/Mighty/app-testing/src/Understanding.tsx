import React from "react"

function Understanding() { 
    return (<div>
        React is a component-based language. So a lot of the things we see in the website will actually be compartmentalized as reusable containers.
    </div>
    ); 
}

export default Understanding; 